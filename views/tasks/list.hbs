{{>head}}
<body>
    {{>header}}
    <main>
        <div class="task-list">
            <div class="search-task">
                {{#if search}}
                <form action="/tasks" method="GET">
                    <div class="submit-box">
                        <input type="text" name="search" value="{{search}}" disabled>
                        <button type="submit" class="submit-button search">Clear</button>
                    </div>
                </form>
                {{else}}
                <form action="/tasks" method="GET" autocomplete="off">
                    <div class="submit-box">
                        <input type="text" name="search">
                        <button type="submit" class="submit-button search">Search</button>
                    </div>
                </form>
                {{/if}}
            </div>
            {{#if tasks}}    
            {{#each tasks}}
            <div class="task-card">
                <div>
                    <p class="task">Task:{{title}}</p>
                    <p>Deadline:{{deadline}}</p>
                </div>
                <div class="submit-button-box">
                    <form action="/tasks/edit/{{id}}" method="GET">
                        <button type="submit" class="submit-button">Edit</button>
                    </form>
                    <form action="/tasks/delete/{{id}}" method="POST">
                        <button type="submit" class="submit-button">Delete</button>
                    </form>
                </div>
            </div>
            {{/each}}
            {{else}}
            <div class="empty-task">
                NO HIT TASKS
            </div>
            {{/if}}
        </div>
    </main>
</body>
</html>